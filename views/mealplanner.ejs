<!DOCTYPE html>
<html lang="en">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">

<%- include('header'); %>
  <body>
      <!-- <div class="col-12 text-center">
        <h1>My Calendar: <%#= year %></h1>
      </div> -->
      
      <!-- <div class="container mt-4"> -->
        <!-- <h1 class="text-center">Calendar for <%#= year %> - <%#= month %></h1> -->
        <!-- <div class="row">
          <div class="col day-header">Sun</div>
          <div class="col day-header">Mon</div>
          <div class="col day-header">Tue</div>
          <div class="col day-header">Wed</div>
          <div class="col day-header">Thu</div>
          <div class="col day-header">Fri</div>
          <div class="col day-header">Sat</div>
        </div> -->
      
        <%# let dayCounter = 0; %>
        <%# for (let week = 0; week < 6; week++) { %>
          <!-- <div class="row"> -->
            <%# for (let dayOfWeek = 0; dayOfWeek < 7; dayOfWeek++) { %>
              <!-- <div class="col day"> -->
                <%# if (dayCounter < calendarDays.length) { %>
                  <%#= calendarDays[dayCounter] %>
                <%# } %>
              <!-- </div> -->
              <%# dayCounter++; %>
            <%# } %>
          <!-- </div> -->
        <%# } %>
      <!-- </div> -->
      

      
    <h1>Calendar - <%= year %>/<%= month + 1 %></h1>
    <div class="calendar">
      <div class="navigation">
        <a href="/planner?year=<%= year %>&month=<%= month - 1 %>">Previous Month</a>
        <a href="/planner?year=<%= year %>&month=<%= month + 1 %>">Next Month</a>
      </div>
      <table>
        <tr>
          <th>Sun</th>
          <th>Mon</th>
          <th>Tue</th>
          <th>Wed</th>
          <th>Thu</th>
          <th>Fri</th>
          <th>Sat</th>
        </tr>
        <tr>
          <% let currentDay = 1; %>
          <% for (let week = 0; week < 6; week++) { %>
            <% for (let day = 0; day < 7; day++) { %>
              <% if ((week === 0 && day < firstDay) || currentDay > daysInMonth) { %>
                <td></td>
              <% } else { %>
                <td><%= currentDay %></td>
                <% currentDay++; %>
              <% } %>
            <% } %>
            </tr><tr> <!-- Start a new row for each week -->
          <% } %>
        </tr>
      </table>
    </div>









      <%- include('footer'); %>
  </body>

</html>